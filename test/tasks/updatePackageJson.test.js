import updatePackageJson from "../../src/tasks/updatePackageJson";

describe.skip("updatePackageJson()", () => {
    it("should save a package.json with expected shape to the cwd", () => {});
    it("should save a package.json with expected format to the cwd", () => {});
    describe("when special semver ranges are used", () => {});
});
